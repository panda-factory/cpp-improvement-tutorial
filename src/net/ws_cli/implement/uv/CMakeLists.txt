INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${WS_CLI_DIR})

SET(WS_CLI_IMPL_SRC
    session_impl_uv.cpp)

ADD_LIBRARY(ws_cli_impl STATIC ${WS_CLI_IMPL_SRC})

TARGET_LINK_DIRECTORIES(ws_cli_impl PRIVATE ${LIBUV_LIBRARY_DIR})
TARGET_LINK_LIBRARIES(ws_cli_impl PRIVATE)